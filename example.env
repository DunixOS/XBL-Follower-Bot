# Use one real Microsoft compact-JWE/RPS ticket per line.
# This placeholder is intentionally invalid and is only a format example.
# tokens are significantly larger than this
# you should make a tokens.env file and put the actual tokens in there, per line, it can be a tokens.txt file, just rename it to .env

eyJhbGciOiJSU0EtT0FFUCIsImVuYyI6IkExMjhDQkMtSFMyNTYifQ.example.encrypted.payload.tag